'use strict';

/**
 * identity-proof controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::identity-proof.identity-proof');
